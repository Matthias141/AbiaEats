# Supabase (REQUIRED)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...
SUPABASE_SERVICE_ROLE_KEY=eyJ...          # Server-only. NEVER prefix with NEXT_PUBLIC_.

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=AbiaEats

# OPay Account Details (shown to customers during payment)
NEXT_PUBLIC_OPAY_ACCOUNT_NAME=AbiaEats
NEXT_PUBLIC_OPAY_ACCOUNT_NUMBER=xxxx

# Security secrets (REQUIRED in production)
# Generate with: openssl rand -base64 32
CRON_SECRET=replace-with-random-secret      # Authenticates Vercel Cron requests
WEBHOOK_SECRET=replace-with-random-secret   # Guards admin client in webhook handlers

# Rate limiting via Upstash Redis (REQUIRED for login/signup rate limits)
# Create a free Redis database at https://upstash.com → copy REST URL + Token
UPSTASH_REDIS_REST_URL=https://xxx.upstash.io
UPSTASH_REDIS_REST_TOKEN=AXxx...

# Paystack (Phase 2)
# PAYSTACK_SECRET_KEY=sk_test_xxxxx
# NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_xxxxx

# WhatsApp Business API (Phase 2)
# WHATSAPP_API_TOKEN=your-token
# WHATSAPP_PHONE_NUMBER_ID=your-phone-id
